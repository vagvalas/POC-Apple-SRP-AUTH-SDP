# Local credentials for the prototype.
#
# Copy this file to `config.py` and fill in your own Apple ID / password.
# `config.py` is in .gitignore and will NOT be committed. If the file is
# missing at runtime, tvos_download.py falls back to interactive prompts.

EMAIL    = "your_apple_id@example.com"
PASSWORD = "your_password"
